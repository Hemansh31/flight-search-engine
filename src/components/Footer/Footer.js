import { Copyright } from '@material-ui/icons';
import './Footer.css';

export const Footer = (props) => {
    return (
        <div className = 'Footer'><Copyright style={{margin : 'auto'}}/><span style={{margin : 'auto', marginLeft : '0'}}> Hemansh Khaneja</span></div>
    );
};